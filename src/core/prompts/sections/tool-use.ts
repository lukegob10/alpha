export function getSharedToolUseSection(): string {
	return `====

TOOL USE

Use the provider-native tool-calling mechanism and the tools supplied with this request. Tool schemas and runtime policy are authoritative.`
}
