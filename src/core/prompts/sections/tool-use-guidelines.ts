export function getToolUseGuidelinesSection(): string {
	return `# Tool Use Guidelines

Use the narrowest suitable tool when it provides useful evidence or performs authorized work. Incorporate observed results before depending on them.`
}
