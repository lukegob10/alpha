export function getObjectiveSection(): string {
	return `====

OBJECTIVE

Understand the user's objective, gather sufficient evidence, and produce the requested outcome. Prefer direct work for tightly coupled tasks and bounded delegation only when it materially helps. Report the result and verification evidence concisely.`
}
